name: deploy
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: "ursamajorlab/jammy-python:3.12"
    steps:
      -
        name: checkout
        uses: actions/checkout@v4
      -
        name: build
        run: docker compose build
        with:
          python-version: 3.12
      -
        name: up
        run: docker compose up -d
      -
        name: prune
        run: docker image prune